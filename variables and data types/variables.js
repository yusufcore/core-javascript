var n = 5;       // var is not used in modern development.
console.log(n);

let m = 25;
console.log(m);

const w = 100;  // declared values cannot be reassigned.
console.log(w);

m = 250;   // value can be updated after decalaration in block scope.
console.log(m);